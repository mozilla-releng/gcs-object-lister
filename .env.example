# GCS Storage Manager Configuration

GCP_PROJECT=your-gcp-project-id

# Required: GCS bucket name to fetch from
BUCKET_NAME=your-gcs-bucket-name

# Optional: Default prefix filter for objects
GCS_PREFIX=pub/firefox

# Optional: Application host and port
APP_HOST=0.0.0.0
APP_PORT=8080

# Optional: Data directory (inside container)
DATA_DIR=/data/fetches
